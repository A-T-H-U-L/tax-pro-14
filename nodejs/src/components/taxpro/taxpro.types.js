/**
 * Taxprocontroller
 * @typedef {import('./taxpro.controller')} Taxprocontroller
 */

/**
 * TaxProService
 * @typedef {import('./taxpro.service')} TaxProService
 */

/**
 * TaxProValidator
 * @typedef {import('./taxpro.validator')} TaxProValidator
 */
